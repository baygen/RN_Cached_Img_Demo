export default (state = {}, action) => {
  switch (action.type) {
  // case FEED.SET_POSTS:
  //   return {
  //     ...state,
  //     allPosts: action.payload
  //   };
  default:
    return state;
  }
};